<%= form_with(model: challenge, local: true) do |form| %>
  <% if challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(challenge.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
      <% challenge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :challenge_name %>
  </div>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.select :team_id, Team.all.collect { |p| [p.name, p.id] } %>
  </div>

  <div class="field">
    <%= form.label :challenge_type_id %>
    <%= form.select :challenge_type_id, ChallengeType.all.collect { |p| [p.name, p.id] }  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
